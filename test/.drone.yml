kind: pipeline
type: docker
name: build

steps:
- name: build
  image: debian:stable
  commands:
  - apt update && apt install python3-pip -y
  - pip3 install -r requirements.txt
  - make html
  - touch 123
 
- name: build
  image: debian:stable
  commands:
  - ls -al
